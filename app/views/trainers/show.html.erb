<h3>Trainer</h3>
<p>Name: <%= @trainer.name if @trainer.name %></p>
<p>Email: <%= @trainer.email if @trainer.email %></p>
<%= button_to "Create New Pokemon", new_pokemon_path(id: current_trainer.id), :class => "button medium", :method => :get%>


<% @pokemons.each do |pokemon| %>
	<% if pokemon.trainer_id == @trainer.id %>

	  <tr>
	  	<% @pokemon = pokemon %>
	  	<table>
	    <th><%= pokemon.name %></th>
	    <th><%= pokemon.level %></th>
	    <th><%= pokemon.health %></th>
	    </table>
	    <td>  <%= button_to "Damage!", pokemons_path(id: p), :class => "button medium", :method => :get%> </td>


	  </tr>
	<% end %>

	      <% if @trainer.id == current_trainer.id %>
	    <%= button_to "Heal", heal_path(id: @pokemon.id), :class => "button medium", :method => :patch %> 
	    <% end %>
<% end %>
